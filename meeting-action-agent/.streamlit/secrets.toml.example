# Copy to Streamlit Cloud: App settings → Secrets (paste TOML below).
# Local dev: copy to .streamlit/secrets.toml (do not commit).

OPENAI_API_KEY = "sk-your-key"
OPENAI_BASE_URL = "https://api.deepseek.com"
OPENAI_MODEL = "deepseek-chat"

# Optional: force cloud-safe UI (hide Agent / Selenium). Auto-detected on Streamlit Cloud.
# MEETINGMIND_APP_MODE = "web"
